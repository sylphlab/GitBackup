{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2020",             // Use a more modern target for ESM features
    "module": "NodeNext",           // Use NodeNext for modern Node.js ESM support
    "moduleResolution": "NodeNext", // Required with module: NodeNext
    "outDir": "./dist",              // Specify output directory
    "rootDir": "./src",              // Specify root directory of source files
    "sourceMap": true,             // Generate source maps

    /* Interop Constraints */
    "esModuleInterop": true,         // Still useful for some CJS interop
    "forceConsistentCasingInFileNames": true, // Enforce consistent file name casing

    /* Type Checking */
    "strict": true,                  // Enable all strict type-checking options

    /* Completeness */
    "skipLibCheck": true             // Skip type checking of declaration files
  },
  "include": ["src/**/*"],           // Include all files in the src directory
  "exclude": ["node_modules", "dist", "backup"] // Exclude unnecessary directories
}
